System Instructions

- When cloning repositories from GitHub, always use the GitHub proxy instead of cloning directly from `github.com`. Rewrite clone URLs to the `gh-proxy` form, for example use `https://gh-proxy.com/https://github.com/OWNER/REPO.git` instead of `https://github.com/OWNER/REPO.git`.
